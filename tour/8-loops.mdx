---
title: "LOOPS"
slug: "8"
---

import { InstructionsWrapper } from './instructions.tsx'
import { CodeWrapper } from './code.tsx';
import { Navigation } from '../src/components/navigation.tsx'

<InstructionsWrapper>
	<div className="instructions-main">
		# LOOPS

		HookScript has a single loop statement starting with the **`for`** keyword, following your condition and a colon ( `:` ). The colon
		starts a new `block` with its own scope for declaring variables local to that block. All `for` statements are terminated with the **`end`**
		keyword.

		## For Statement
		```
		var x = 0
		for x < 10:
			x++
		end
		print(x)
		```
	</div>

	<Navigation back="/tour/7" forward="/tour/9" index="8" />

</InstructionsWrapper>

<CodeWrapper initialCode={`var x = 0
for x < 10:
	print(x)
	x++
end
`}>
</CodeWrapper>